apiVersion: v1
kind: Service
metadata:
  name: {{ include "authsys-frontend-chart.fullname" . }}
  labels:
    {{- include "authsys-frontend-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "authsys-frontend-chart.selectorLabels" . | nindent 4 }}
