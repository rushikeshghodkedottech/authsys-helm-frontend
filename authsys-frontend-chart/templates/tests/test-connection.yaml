apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "authsys-frontend-chart.fullname" . }}-test-connection"
  labels:
    {{- include "authsys-frontend-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "authsys-frontend-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
